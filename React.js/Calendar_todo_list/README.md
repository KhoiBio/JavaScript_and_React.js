# Calendar To-Do App (React)

A simple **calendar-based to-do app** built with **React**, allowing you to:

- Navigate months  
- Click a day to view/add/delete tasks  
- Mark tasks as completed with a checkbox  
- Persist tasks using **localStorage**  

This app is **portfolio-ready** and uses **component-based React** for easy maintenance.

---

## Features

- **Calendar with month navigation**  
- **Add / Delete / Complete tasks**  
- Completed tasks appear **strikethrough**  
- Tasks persist across page reloads using **localStorage**  
- Days with pending tasks are bold on the calendar  
- Fully **reactive UI** with automatic updates

---

## Option 1: Local Setup (Using Create React App)

### 1. Install Node.js

Download from [nodejs.org](https://nodejs.org) and install it.  
Check installation:

```bash
node -v
npm -v
